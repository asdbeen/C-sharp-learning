<Window x:Class="ICommandInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ICommandInterface"
        xmlns:viewmodel="clr-namespace:ICommandInterface.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <viewmodel:ButtonViewModel x:Key="vm"> </viewmodel:ButtonViewModel>
    </Window.Resources>
    
    <Grid>
        <StackPanel Width="200" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Height="80" FontSize="36" Background="LightBlue" Content="Click Me" Command="{Binding Source={StaticResource vm},Path=ButtonCommand}"> </Button>
        </StackPanel>
    </Grid>
</Window>
